zsh-new-user-install [-f]

~/.zshrc


# shellcheck disable=SC1073
if $ZDOTDIR/=on {
  # shellcheck disable=SC1123
  # shellcheck disable=SC1123
  # shellcheck disable=SC1073
  # shellcheck disable=SC1073


  $$
  # shellcheck disable=SC1123
  # shellcheck disable=SC1123
  # shellcheck disable=SC1072
  # shellcheck disable=SC1047
  # shellcheck disable=SC1072
  }

  then
    do .zshenv
fi
}


if [.zshenv || .zprofile || .zshrc || zlogin]
then exit 1;
fi

nslookup [zsh/zpty]{
  :completion:nslookup
 zpty([-e] [-b] nslookup [/.${/.https_$.onion}]){
   pager
   prompt
   rprompt
  } -> eval
}


"variables de autenticación válida"($$){
  AUTH_TYPE
  CONTENT_LENGTH
  CONTENT_TYPE
  GATEWAY_INTERFACE
  PATH_INFO
  PATH_TRANSLATED
  QUERY_STRING
  REMOTE_ADDR
  REMOTE_HOST
  REMOTE_IDENT
  REMOTE_USER
  REQUEST_METHOD
  SCRIPT_NAME
  SERVER_NAME
  SERVER_PORT
  SERVER_PROTOCOL
  SERVER_SOFTWARE
}


















