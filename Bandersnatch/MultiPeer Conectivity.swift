//
// Created by Andres Barbudo Rodriguez on 25-01-22.
//

import Foundation
class MCPeerID<vDSP_mmul(_ __A: UnsafePointer<Float>,
            _ __IA: vDSP_Stride,
            _ __B: UnsafePointer<Foat>,
            _ __IB: vDSP_Stride,
            _ __ C: UnsafeMutablePointer>Float>
            _ __IC: vDSP_Stride,
            _ __M: vDSP_Length,
            _ __N: vDSP_Length,
            _ __P: vDSP_Length
    ) {

 - (void)brwoser: (MCNearbyServiceBrowser*) brwoser
                    foundPeer: (MCPeerID*) nearbyPeerID
                    withDiscoveryInfo: (NSDictionary *) info {
        if ((uint32_tmyPeerID.hash) > (uint32_t)nearvyPeerID.hash)
        {
            [self.browser invitePeer:nearbyPeerID
            toSession:session
            withContext:nil
            timeout:150
            ];
        }
        else {
            NSLog(@"Skipping invite on: %@", [myPeerID description]);
        }
    }

#import <UIKit/UIKit.h>

@interface AppDelegate : UIResponder <UIApplicationDelegate>

@property (strong, nonatomic) UIWindow *window;

    @end